# Captured from a live `container` install — the authority for the Allowlist.
#
# Generated by Scripts/capture-cli-help.sh on a Mac with the CLI present. The agent VMs
# have no `container`, so this file is how a flag audit gets real evidence instead of
# documentation that has already proved wrong in places.
#
# version: container CLI version 1.0.0 (build: release, commit: ee848e3)

===== container  =====
OVERVIEW: A container platform for macOS

USAGE: container [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


CONTAINER SUBCOMMANDS:
  copy, cp                Copy files/folders between a container and the local
                          filesystem
  create                  Create a new container
  delete, rm              Delete one or more containers
  exec                    Run a new command in a running container
  export                  Export a container's filesystem as a tar archive
  inspect                 Display information about one or more containers
  kill                    Kill or signal one or more running containers
  list, ls                List running containers
  logs                    Fetch container logs
  run                     Run a container
  start                   Start a container
  stats                   Display resource usage statistics for containers
  stop                    Stop one or more running containers
  prune                   Remove all stopped containers


IMAGE SUBCOMMANDS:
  build                   Build an image from a Dockerfile or Containerfile
  image, i                Manage images
  registry, r             Manage registry logins


MACHINE SUBCOMMANDS:
  machine, m              Manage container machines


VOLUME SUBCOMMANDS:
  volume, v               Manage container volumes


OTHER SUBCOMMANDS:
  builder                 Manage an image builder instance
  network, n              Manage container networks
  system, s               Manage system components


===== container run =====
OVERVIEW: Run a container

USAGE: container run [<options>] <image> [<arguments> ...]

ARGUMENTS:
  <image>                 Image name
  <arguments>             Container init process arguments

PROCESS OPTIONS:
  -e, --env <env>         Set environment variables (key=value, or just key to
                          inherit from host)
  --env-file <env-file>   Read in a file of environment variables (key=value
                          format, ignores # comments and blank lines)
  --gid <gid>             Set the group ID for the process
  -i, --interactive       Keep the standard input open even if not attached
  -t, --tty               Open a TTY with the process
  -u, --user <user>       Set the user for the process (format: name|uid[:gid])
  --uid <uid>             Set the user ID for the process
  -w, --workdir, --cwd <dir>
                          Set the initial working directory inside the container
  --ulimit <limit>        Set resource limits (format: <type>=<soft>[:<hard>])

RESOURCE OPTIONS:
  -c, --cpus <cpus>       Number of CPUs to allocate to the container
  -m, --memory <memory>   Amount of memory (1MiByte granularity), with optional
                          K, M, G, T, or P suffix

MANAGEMENT OPTIONS:
  -a, --arch <arch>       Set arch if image can target multiple architectures
                          (default: arm64)
  --cap-add <cap>         Add a Linux capability (e.g. CAP_NET_RAW, or ALL)
  --cap-drop <cap>        Drop a Linux capability (e.g. CAP_NET_RAW, or ALL)
  --cidfile <cidfile>     Write the container ID to the path provided
  -d, --detach            Run the container and detach from the process
  --dns <ip>              DNS nameserver IP address
  --dns-domain <domain>   Default DNS domain
  --dns-option <option>   DNS options
  --dns-search <domain>   DNS search domains
  --entrypoint <cmd>      Override the entrypoint of the image
  --init                  Run an init process inside the container that
                          forwards signals and reaps processes
  --init-image <image>    Use a custom init image instead of the default
  -k, --kernel <path>     Set a custom kernel path
  -l, --label <label>     Add a key=value label to the container
  --mount <mount>         Add a mount to the container (format:
                          type=<>,source=<>,target=<>,readonly)
  --name <name>           Use the specified name as the container ID
  --network <network>     Attach the container to a network (format:
                          <name>[,mac=XX:XX:XX:XX:XX:XX][,mtu=VALUE])
  --no-dns                Do not configure DNS in the container
  --os <os>               Set OS if image can target multiple operating systems
                          (default: linux)
  -p, --publish <spec>    Publish a port from container to host (format:
                          [host-ip:]host-port:container-port[/protocol])
  --platform <platform>   Platform for the image if it's multi-platform. This
                          takes precedence over --os and --arch [environment:
                          CONTAINER_DEFAULT_PLATFORM]
  --publish-socket <spec> Publish a socket from container to host (format:
                          host_path:container_path)
  --read-only             Mount the container's root filesystem as read-only
  --rm, --remove          Remove the container after it stops
  --rosetta               Enable Rosetta in the container
  --runtime <runtime>     Set the runtime handler for the container (default:
                          container-runtime-linux)
  --ssh                   Forward SSH agent socket to container
  --shm-size <shm-size>   Size of /dev/shm (e.g. 64M, 1G)
  --tmpfs <tmpfs>         Add a tmpfs mount to the container at the given path
  --virtualization        Expose virtualization capabilities to the container
                          (requires host and guest support)
  -v, --volume <volume>   Bind mount a volume into the container

REGISTRY OPTIONS:
  --scheme <scheme>       Scheme to use when connecting to the container
                          registry. One of (http, https, auto) (default: auto)

PROGRESS OPTIONS:
  --progress <type>       Progress type (format: auto|none|ansi|plain|color)
                          (default: auto)

IMAGE FETCH OPTIONS:
  --max-concurrent-downloads <max-concurrent-downloads>
                          Maximum number of concurrent downloads (default: 3)
                          (default: 3)

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container create =====
OVERVIEW: Create a new container

USAGE: container create [<options>] <image> [<arguments> ...]

ARGUMENTS:
  <image>                 Image name
  <arguments>             Container init process arguments

PROCESS OPTIONS:
  -e, --env <env>         Set environment variables (key=value, or just key to
                          inherit from host)
  --env-file <env-file>   Read in a file of environment variables (key=value
                          format, ignores # comments and blank lines)
  --gid <gid>             Set the group ID for the process
  -i, --interactive       Keep the standard input open even if not attached
  -t, --tty               Open a TTY with the process
  -u, --user <user>       Set the user for the process (format: name|uid[:gid])
  --uid <uid>             Set the user ID for the process
  -w, --workdir, --cwd <dir>
                          Set the initial working directory inside the container
  --ulimit <limit>        Set resource limits (format: <type>=<soft>[:<hard>])

RESOURCE OPTIONS:
  -c, --cpus <cpus>       Number of CPUs to allocate to the container
  -m, --memory <memory>   Amount of memory (1MiByte granularity), with optional
                          K, M, G, T, or P suffix

MANAGEMENT OPTIONS:
  -a, --arch <arch>       Set arch if image can target multiple architectures
                          (default: arm64)
  --cap-add <cap>         Add a Linux capability (e.g. CAP_NET_RAW, or ALL)
  --cap-drop <cap>        Drop a Linux capability (e.g. CAP_NET_RAW, or ALL)
  --cidfile <cidfile>     Write the container ID to the path provided
  -d, --detach            Run the container and detach from the process
  --dns <ip>              DNS nameserver IP address
  --dns-domain <domain>   Default DNS domain
  --dns-option <option>   DNS options
  --dns-search <domain>   DNS search domains
  --entrypoint <cmd>      Override the entrypoint of the image
  --init                  Run an init process inside the container that
                          forwards signals and reaps processes
  --init-image <image>    Use a custom init image instead of the default
  -k, --kernel <path>     Set a custom kernel path
  -l, --label <label>     Add a key=value label to the container
  --mount <mount>         Add a mount to the container (format:
                          type=<>,source=<>,target=<>,readonly)
  --name <name>           Use the specified name as the container ID
  --network <network>     Attach the container to a network (format:
                          <name>[,mac=XX:XX:XX:XX:XX:XX][,mtu=VALUE])
  --no-dns                Do not configure DNS in the container
  --os <os>               Set OS if image can target multiple operating systems
                          (default: linux)
  -p, --publish <spec>    Publish a port from container to host (format:
                          [host-ip:]host-port:container-port[/protocol])
  --platform <platform>   Platform for the image if it's multi-platform. This
                          takes precedence over --os and --arch [environment:
                          CONTAINER_DEFAULT_PLATFORM]
  --publish-socket <spec> Publish a socket from container to host (format:
                          host_path:container_path)
  --read-only             Mount the container's root filesystem as read-only
  --rm, --remove          Remove the container after it stops
  --rosetta               Enable Rosetta in the container
  --runtime <runtime>     Set the runtime handler for the container (default:
                          container-runtime-linux)
  --ssh                   Forward SSH agent socket to container
  --shm-size <shm-size>   Size of /dev/shm (e.g. 64M, 1G)
  --tmpfs <tmpfs>         Add a tmpfs mount to the container at the given path
  --virtualization        Expose virtualization capabilities to the container
                          (requires host and guest support)
  -v, --volume <volume>   Bind mount a volume into the container

REGISTRY OPTIONS:
  --scheme <scheme>       Scheme to use when connecting to the container
                          registry. One of (http, https, auto) (default: auto)

IMAGE FETCH OPTIONS:
  --max-concurrent-downloads <max-concurrent-downloads>
                          Maximum number of concurrent downloads (default: 3)
                          (default: 3)

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container start =====
OVERVIEW: Start a container

USAGE: container start [--attach] [--interactive] [--debug] <container-id>

ARGUMENTS:
  <container-id>          Container ID

OPTIONS:
  -a, --attach            Attach stdout/stderr
  -i, --interactive       Attach stdin
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container stop =====
OVERVIEW: Stop one or more running containers

USAGE: container stop [--all] [--signal <signal>] [--time <time>] [--debug] [<container-ids> ...]

ARGUMENTS:
  <container-ids>         Container IDs

OPTIONS:
  -a, --all               Stop all running containers
  -s, --signal <signal>   Signal to send to the containers
  -t, --time <time>       Seconds to wait before killing the containers
                          (default: 5)
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container kill =====
OVERVIEW: Kill or signal one or more running containers

USAGE: container kill [--all] [--signal <signal>] [--debug] [<container-ids> ...]

ARGUMENTS:
  <container-ids>         Container IDs

OPTIONS:
  -a, --all               Kill or signal all running containers
  -s, --signal <signal>   Signal to send to the container(s) (default: KILL)
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container delete =====
OVERVIEW: Delete one or more containers

USAGE: container delete [--all] [--force] [--debug] [<container-ids> ...]

ARGUMENTS:
  <container-ids>         Container IDs

OPTIONS:
  -a, --all               Delete all containers
  -f, --force             Delete containers even if they are running
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container list =====
OVERVIEW: List running containers

USAGE: container list [--all] [--format <format>] [--quiet] [--debug]

OPTIONS:
  -a, --all               Include containers that are not running
  --format <format>       Format of the output (values: json, table, yaml,
                          toml; default: table)
  -q, --quiet             Only output the container ID
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container inspect =====
OVERVIEW: Display information about one or more containers

USAGE: container inspect [--debug] <container-ids> ...

ARGUMENTS:
  <container-ids>         Container IDs to inspect

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container logs =====
OVERVIEW: Fetch container logs

USAGE: container logs [--boot] [--follow] [-n <n>] [--debug] <container-id>

ARGUMENTS:
  <container-id>          Container ID

OPTIONS:
  --boot                  Display the boot log for the container instead of
                          stdio
  -f, --follow            Follow log output
  -n <n>                  Number of lines to show from the end of the logs. If
                          not provided this will print all of the logs
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container exec =====
OVERVIEW: Run a new command in a running container

USAGE: container exec [<options>] <container-id> <arguments> ...

ARGUMENTS:
  <container-id>          Container ID
  <arguments>             New process arguments

PROCESS OPTIONS:
  -e, --env <env>         Set environment variables (key=value, or just key to
                          inherit from host)
  --env-file <env-file>   Read in a file of environment variables (key=value
                          format, ignores # comments and blank lines)
  --gid <gid>             Set the group ID for the process
  -i, --interactive       Keep the standard input open even if not attached
  -t, --tty               Open a TTY with the process
  -u, --user <user>       Set the user for the process (format: name|uid[:gid])
  --uid <uid>             Set the user ID for the process
  -w, --workdir, --cwd <dir>
                          Set the initial working directory inside the container
  --ulimit <limit>        Set resource limits (format: <type>=<soft>[:<hard>])

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  -d, --detach            Run the process and detach from it
  --version               Show the version.
  -h, --help              Show help information.


===== container prune =====
OVERVIEW: Remove all stopped containers

USAGE: container prune [--debug]

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container copy =====
OVERVIEW: Copy files/folders between a container and the local filesystem

USAGE: container copy [--debug] <source> <destination>

ARGUMENTS:
  <source>                Source path (container:path or local path)
  <destination>           Destination path (container:path or local path)

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.


===== container export =====
OVERVIEW: Export a container's filesystem as a tar archive

USAGE: container export [--debug] [--output <output>] <id>

ARGUMENTS:
  <id>                    container ID

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  -o, --output <output>   Pathname for the saved container filesystem (defaults
                          to stdout)
  --version               Show the version.
  -h, --help              Show help information.


===== container build =====
OVERVIEW: Build an image from a Dockerfile or Containerfile

USAGE: container build [<options>] [<context-dir>]

ARGUMENTS:
  <context-dir>           Build directory (default: .)

OPTIONS:
  -a, --arch <value>      Add the architecture type to the build
  --build-arg <key=val>   Set build-time variables
  -c, --cpus <cpus>       Number of CPUs to allocate to the builder container
  -f, --file <path>       Path to Dockerfile
  -l, --label <key=val>   Set a label
  -m, --memory <memory>   Amount of builder container memory (1MiByte
                          granularity), with optional K, M, G, T, or P suffix
  --no-cache              Do not use cache
  -o, --output <value>    Output configuration for the build (format:
                          type=<oci|tar|local>[,dest=]) (default: type=oci)
  --os <value>            Add the OS type to the build
  --platform <platform>   Add the platform to the build (format:
                          os/arch[/variant], takes precedence over --os and
                          --arch) [environment: CONTAINER_DEFAULT_PLATFORM]
  --progress <type>       Progress type (format: auto|plain|tty) (default: auto)
  -q, --quiet             Suppress build output
  --secret <id=key,...>   Set build-time secrets (format:
                          id=<key>[,env=<ENV_VAR>|,src=<local/path>])
  -t, --tag <name>        Name for the built image (default:
                          498dc53d-924c-48ae-94b0-a107913ffb3d)
  --target <stage>        Set the target build stage
  --vsock-port <port>     Builder shim vsock port (default: 8088)
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --dns <ip>              DNS nameserver IP address
  --dns-domain <domain>   Default DNS domain
  --dns-option <option>   DNS options
  --dns-search <domain>   DNS search domains
  --pull                  Pull latest image
  --version               Show the version.
  -h, --help              Show help information.


===== container image =====
OVERVIEW: Manage images

USAGE: container image [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  delete, rm              Delete one or more images
  inspect                 Display information about one or more images
  list, ls                List images
  load                    Load images from an OCI compatible tar archive
  prune                   Remove unused or all images
  pull                    Pull an image
  push                    Push an image
  save                    Save one or more images as an OCI compatible tar
                          archive
  tag                     Create a new reference for an existing image

  See 'container help image <subcommand>' for detailed help.

===== container image pull =====
Error: Plugin 'container-image pull' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image pull
  - /usr/local/libexec/container/plugins/image pull

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image push =====
Error: Plugin 'container-image push' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image push
  - /usr/local/libexec/container/plugins/image push

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image list =====
Error: Plugin 'container-image list' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image list
  - /usr/local/libexec/container/plugins/image list

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image inspect =====
Error: Plugin 'container-image inspect' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image inspect
  - /usr/local/libexec/container/plugins/image inspect

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image delete =====
Error: Plugin 'container-image delete' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image delete
  - /usr/local/libexec/container/plugins/image delete

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image tag =====
Error: Plugin 'container-image tag' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image tag
  - /usr/local/libexec/container/plugins/image tag

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image save =====
Error: Plugin 'container-image save' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image save
  - /usr/local/libexec/container/plugins/image save

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image load =====
Error: Plugin 'container-image load' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image load
  - /usr/local/libexec/container/plugins/image load

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container image prune =====
Error: Plugin 'container-image prune' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/image prune
  - /usr/local/libexec/container/plugins/image prune

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container volume =====
OVERVIEW: Manage container volumes

USAGE: container volume [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  create                  Create a new volume
  delete, rm              Delete one or more volumes
  list, ls                List volumes
  inspect                 Display information about one or more volumes
  prune                   Remove volumes with no container references

  See 'container help volume <subcommand>' for detailed help.

===== container volume create =====
Error: Plugin 'container-volume create' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/volume create
  - /usr/local/libexec/container/plugins/volume create

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container volume delete =====
Error: Plugin 'container-volume delete' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/volume delete
  - /usr/local/libexec/container/plugins/volume delete

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container volume list =====
Error: Plugin 'container-volume list' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/volume list
  - /usr/local/libexec/container/plugins/volume list

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container volume inspect =====
Error: Plugin 'container-volume inspect' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/volume inspect
  - /usr/local/libexec/container/plugins/volume inspect

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container volume prune =====
Error: Plugin 'container-volume prune' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/volume prune
  - /usr/local/libexec/container/plugins/volume prune

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container network =====
OVERVIEW: Manage container networks

USAGE: container network [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  create                  Create a new network
  delete, rm              Delete one or more networks
  list, ls                List networks
  inspect                 Display information about one or more networks
  prune                   Remove networks with no container connections

  See 'container help network <subcommand>' for detailed help.

===== container network create =====
Error: Plugin 'container-network create' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/network create
  - /usr/local/libexec/container/plugins/network create

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container network delete =====
Error: Plugin 'container-network delete' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/network delete
  - /usr/local/libexec/container/plugins/network delete

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container network list =====
Error: Plugin 'container-network list' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/network list
  - /usr/local/libexec/container/plugins/network list

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container network inspect =====
Error: Plugin 'container-network inspect' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/network inspect
  - /usr/local/libexec/container/plugins/network inspect

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container network prune =====
Error: Plugin 'container-network prune' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/network prune
  - /usr/local/libexec/container/plugins/network prune

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container system =====
OVERVIEW: Manage system components

USAGE: container system [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  df                      Show disk usage for images, containers, and volumes
  dns                     Manage local DNS domains
  kernel                  Manage the default kernel configuration
  logs                    Fetch system logs for `container` services
  property                Manage system property values
  start                   Start `container` services
  status                  Show the status of `container` services
  stop                    Stop all `container` services
  version                 Show version information

  See 'container help system <subcommand>' for detailed help.

===== container system start =====
Error: Plugin 'container-system start' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/system start
  - /usr/local/libexec/container/plugins/system start

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container system stop =====
Error: Plugin 'container-system stop' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/system stop
  - /usr/local/libexec/container/plugins/system stop

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container system status =====
Error: Plugin 'container-system status' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/system status
  - /usr/local/libexec/container/plugins/system status

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container system logs =====
Error: Plugin 'container-system logs' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/system logs
  - /usr/local/libexec/container/plugins/system logs

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container system df =====
Error: Plugin 'container-system df' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/system df
  - /usr/local/libexec/container/plugins/system df

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container system property =====
Error: Plugin 'container-system property' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/system property
  - /usr/local/libexec/container/plugins/system property

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container registry =====
OVERVIEW: Manage registry logins

USAGE: container registry [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  login                   Log in to a registry
  logout                  Log out from a registry
  list, ls                List image registry logins

  See 'container help registry <subcommand>' for detailed help.

===== container registry login =====
Error: Plugin 'container-registry login' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/registry login
  - /usr/local/libexec/container/plugins/registry login

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container registry logout =====
Error: Plugin 'container-registry logout' not found.

- If system services are not running, start them with: container system start
- If the plugin isn't installed, ensure it exists under:

Check to see that the plugin exists under:
  - /usr/local/libexec/container-plugins/registry logout
  - /usr/local/libexec/container/plugins/registry logout

Usage: container [--debug] <subcommand>
  See 'container --help' for more information.

===== container builder =====
OVERVIEW: Manage an image builder instance

USAGE: container builder [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  start                   Start the builder container
  status                  Display the builder container status
  stop                    Stop the builder container
  delete, rm              Delete the builder container

  See 'container help builder <subcommand>' for detailed help.

===== container machine =====
OVERVIEW: Manage container machines

EXAMPLES:
  List available images and create a container machine:
    $ container machine create alpine:3.22 --name my-machine

  Run commands in the container machine:
    $ container machine run -n my-machine uname
    $ container machine run -n my-machine -- cat /proc/cpuinfo

  Change the container machine configuration (takes effect after restart):
    $ container machine set -n my-machine cpus=4 memory=8G home-mount=ro
    $ container machine stop my-machine
    $ container machine run -n my-machine -- nproc

  Stop and delete the container machine:
    $ container machine stop my-machine
    $ container machine delete my-machine

USAGE: container machine [--debug] <subcommand>

OPTIONS:
  --debug                 Enable debug output [environment: CONTAINER_DEBUG]
  --version               Show the version.
  -h, --help              Show help information.

SUBCOMMANDS:
  create                  Create a new container machine and boot it
  delete, rm              Delete a container machine
  inspect                 Display detailed information about a container machine
  list, ls                List container machines
  logs                    Fetch container machine logs
  run                     Run a command or interactive shell in a container
                          machine, booting the container machine if necessary
  set                     Set container machine configuration values
  set-default             Set the default container machine
  stop                    Stop a running container machine

  See 'container help machine <subcommand>' for detailed help.

